spring:
  datasource:
    url: jdbc:sqlite:target/test-health.db
    driver-class-name: org.sqlite.JDBC
  jpa:
    database-platform: org.hibernate.community.dialect.SQLiteDialect
    hibernate:
      ddl-auto: create-drop

app:
  cors:
    allowed-origins: "http://localhost:5173"
